# Assets

Sustituye estos dos archivos por los reales, manteniendo exactamente el mismo nombre. No hace falta tocar código.

- `video-sorpresa.mp4` — el vídeo del "admirador secreto". Formato recomendado: H.264/mp4, comprimido a menos de 20-30MB para que cargue rápido en móvil (`ffmpeg -i original.mov -crf 28 video-sorpresa.mp4`).
- `regalo-final.jpg` — la foto del regalo real que se revela al final.

Tras sustituir, haz commit y push a `main`. Vercel redespliega automáticamente en unos segundos y el link ya sirve los archivos nuevos.
